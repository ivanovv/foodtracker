- title t('.title')

%p
  %strong= t('categories.name') + ":"
  =h @category.name

%p
  = link_to t(:edit), edit_category_path(@category)
  |
  = link_to t(:destroy), @category, :confirm => 'Are you sure?', :method => :delete
  |
  = link_to t(:view_all), categories_path

