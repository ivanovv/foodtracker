- semantic_form_for @calc, :url => {:action => "update" } do |f|
  -# = f.error_messages
  %p
    - f.inputs do
      = f.input :weight, :as => :numeric

    -#= f.label :weight
    -#%br
    -#= f.text_field :weight
  %p
    -#= f.submit "Submit"
    - f.buttons do
      = f.commit_button :label => "Calculate!"

